const container = {padding: '15px 0'};

const alignCenter = {textAlign: 'center'};

const submitButton = {
  background: 'rgb(89, 156, 4)',
  margin: '0 5px',
};

const cancelButton = {
  background: 'rgb(220, 40, 40)',
  margin: '0 5px'
};

const button = {background: 'rgb(89, 156, 4)',};

const hidden = {display: 'none'};

export default {
  container,
  alignCenter,
  submitButton,
  cancelButton,
  button,
  hidden,
};