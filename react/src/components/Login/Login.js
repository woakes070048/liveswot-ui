import React from 'react';

const Login = (props) => {
  return (
    <form>
      <input type='email' />
      <input type='password' />
      <input type='submit' value='Login' />
    </form>
  );
};

export default Login;
